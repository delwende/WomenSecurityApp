<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".HomeActivity" >

   
  
    <ImageButton android:id="@+id/panic_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/panic_selector"
        android:layout_centerInParent="true"
        android:background="@android:color/transparent"/>
    
     <com.tavant.droid.security.widget.CustomizableTextView android:id="@+id/message"
        android:layout_below="@id/panic_button"
        android:layout_marginTop="20dip"
        style="@style/settings_bold"
        android:textSize="16sp"
        android:layout_centerHorizontal="true"
        android:text="@string/press_button"/>
    
 
</RelativeLayout>
